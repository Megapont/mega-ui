const variants = {
  accent: () => ({
    container: {
      bg: 'linear(to-b, primaryGradient.900, primary.900)',
    },
  }),
};

const defaultProps = {
  colorScheme: 'brand',
};

export default {
  variants,
  defaultProps,
};
